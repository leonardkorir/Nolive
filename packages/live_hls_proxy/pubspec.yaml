name: live_hls_proxy
description: Pure Dart package for LL-HLS local loopback proxies.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  meta: ^1.9.0
  crypto: ^3.0.6
  live_core:
    path: ../live_core
  live_providers:
    path: ../live_providers

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.8
  fake_async: ^1.3.1
